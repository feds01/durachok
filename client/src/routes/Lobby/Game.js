import React from 'react';
import PropTypes from 'prop-types';

const Game = props => {
    return (
        <div>
            Game wooho!

        </div>
    );
};

Game.propTypes = {

};

export default Game;
